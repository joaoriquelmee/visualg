algoritmo "Sequência Fibonacci"
var cont, N1, N2, soma: inteiro

inicio
      // Sequência Fibonacci//
      N1 <- 0
      N2 <- 1
      Para cont <- 0 ate 14 faca
      Escreva(soma)
      N1 <- (N2)
      N2 <- (soma)
      Soma <- N1 + N2
      FimPara
fimalgoritmo

//Não entendi muito bem, revisar caso precisar no futuro!
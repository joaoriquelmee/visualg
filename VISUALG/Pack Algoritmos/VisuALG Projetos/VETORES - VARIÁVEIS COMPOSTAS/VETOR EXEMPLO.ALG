algoritmo "Vetor"
var num: vetor[1..4] de inteiro
cont: inteiro

inicio
      Para cont <- 1 ate 4 faca
      Escreva("Qual número quer armazenar na posição", cont, " ? :")
      Leia(num[cont])
      FimPara
fimalgoritmo

//Exemplo de uso de vetores